<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      span {
        color: chocolate;
        font-weight: 600;
      }
      div {
        width: 99%;
        height: 30px;
        border: 1px solid darkslategray;
        border-radius: 5px;
        display: flex;
        align-items: center;
        padding: 5px;
        margin-bottom: 5px;
      }
    </style>
  </head>
  <body>
    <h2>결과 확인하기</h2>
    <p><span> <%=userInfo.name%></span> 님 환영합니다!</p>
    <!-- 수업에서는 아래를 ul로 함  -->
    <div>성별 : <%=userInfo.gender%></div>

    <div>
      <p><%=userInfo.year%>년 <%=userInfo.month%>월 <%=userInfo.date%> 일</p>
    </div>

    <div>
      <p>관심사 : <%=userInfo.interest%></p>
    </div>

    <% if(addInfo){ %>
    <h3>다른 태그 작성해보기</h3>

    <p>
      좋아하는 색 :
      <span class="color"><%=userInfo.color%></span>
    </p>
    <p>좋아하는 숫자 : <%=userInfo.number%></p>
    <p>다짐 한마디: <%=userInfo.comment%></p>

    <p>하루 중 가장 좋아하는 시간 : <%=userInfo.time%></p>

    <% } %>
    <!-- 아래는 좋아하는 색으로 글자 색도 지정!!!! 다시!!! -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const colorSpan = document.querySelector(".color");
        colorSpan.style.color = "<%=userInfo.color%>";
      });
    </script>

    <!-- prac2 -->
    <!-- name: '',
  year: '2024',
  month: '1',
  day: '1',
  color: '#000000',
  number: '',
  comment: '',
  time: ''
} 
addInfo :true 일때 if문으로 보에게 함 practice1은 false
-->

    <a href="/">HOME</a>
  </body>
</html>
